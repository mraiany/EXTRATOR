requests
python-dotenv==1.0.1
aiohttp==3.9.1
selenium-wire==5.1.0
# selenium-wire depende de blinker._saferef, removido a partir de 1.8.x.
blinker<1.8.0
# Versão não-yankada de undetected-chromedriver. A 3.3.1 foi removida do PyPI, por isso usamos 3.5.5.
undetected-chromedriver==3.5.5
